const fs = require("fs")
module.exports = async () => {
    try {   
        
        return {  }
    } catch (error) {
        throw new Error("Failed to establish connection to Files: " + err.message);
    }
}